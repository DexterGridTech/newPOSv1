import {UiVariable} from "@impos2/kernel-core-navigation";

export const uiIntegrationDesktopUiVariables:Record<string, UiVariable<any>> = {
    rootScreenContainer:{key:"rootScreenContainer",defaultValue:null}
}