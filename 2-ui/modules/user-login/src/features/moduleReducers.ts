import { ReducersMapObject } from "@reduxjs/toolkit";

/**
 * 用户登录UI 模块 Redux Reducers 汇总
 *
 * 将所有 slice 的 reducer 汇总到这里
 */
export const moduleReducers: ReducersMapObject = {
    // 在这里添加你的 reducers
    // 例如: [mySlice.name]: mySlice.reducer,
};
