# 监控大屏模块前端设计

## 一、模块概述

监控大屏模块提供终端设备的实时监控、告警管理和统计分析功能，支持大屏展示和数据可视化。

## 二、页面结构

```
监控大屏
├── 实时监控 (/monitor/realtime)
├── 告警中心 (/monitor/alerts)
└── 统计报表 (/monitor/statistics)
```

## 三、实时监控页面

### 3.1 监控大屏

```typescript
// src/pages/Monitor/Realtime/index.tsx
import React, { useEffect, useState } from 'react'
import { Card, Row, Col, Statistic, Table, Tag } from 'antd'
import { ArrowUpOutlined, ArrowDownOutlined } from '@ant-design/icons'
import { Line, Pie, Column } from '@ant-design/charts'

const RealtimeMonitor: React.FC = () => {
  const [stats, setStats] = useState({
    totalTerminals: 0,
    onlineTerminals: 0,
    offlineTerminals: 0,
    faultTerminals: 0,
    onlineRate: 0,
  })

  // WebSocket实时更新
  useEffect(() => {
    const ws = new WebSocket(`${WS_URL}/monitor/realtime`)

    ws.onmessage = (event) => {
      const data = JSON.parse(event.data)
      setStats(data)
    }

    return () => ws.close()
  }, [])

  return (
    <div style={{ background: '#0a1929', minHeight: '100vh', padding: 24 }}>
      {/* 顶部统计卡片 */}
      <Row gutter={16} style={{ marginBottom: 24 }}>
        <Col span={6}>
          <Card style={{ background: '#1e3a5f' }}>
            <Statistic
              title={<span style={{ color: '#fff' }}>终端总数</span>}
              value={stats.totalTerminals}
              valueStyle={{ color: '#fff' }}
            />
          </Card>
        </Col>
        <Col span={6}>
          <Card style={{ background: '#1e3a5f' }}>
            <Statistic
              title={<span style={{ color: '#fff' }}>在线终端</span>}
              value={stats.onlineTerminals}
              valueStyle={{ color: '#52c41a' }}
              prefix={<ArrowUpOutlined />}
            />
          </Card>
        </Col>
        <Col span={6}>
          <Card style={{ background: '#1e3a5f' }}>
            <Statistic
              title={<span style={{ color: '#fff' }}>离线终端</span>}
              value={stats.offlineTerminals}
              valueStyle={{ color: '#faad14' }}
              prefix={<ArrowDownOutlined />}
            />
          </Card>
        </Col>
        <Col span={6}>
          <Card style={{ background: '#1e3a5f' }}>
            <Statistic
              title={<span style={{ color: '#fff' }}>在线率</span>}
              value={stats.onlineRate}
              precision={2}
              valueStyle={{ color: '#1890ff' }}
              suffix="%"
            />
          </Card>
        </Col>
      </Row>

      {/* 图表区域 */}
      <Row gutter={16} style={{ marginBottom: 24 }}>
        <Col span={12}>
          <Card title="终端状态分布" style={{ background: '#1e3a5f' }}>
            <TerminalStatusPieChart />
          </Card>
        </Col>
        <Col span={12}>
          <Card title="设备类型分布" style={{ background: '#1e3a5f' }}>
            <DeviceTypeColumnChart />
          </Card>
        </Col>
      </Row>

      <Row gutter={16}>
        <Col span={24}>
          <Card title="在线趋势" style={{ background: '#1e3a5f' }}>
            <OnlineTrendLineChart />
          </Card>
        </Col>
      </Row>

      {/* 告警列表 */}
      <Row gutter={16} style={{ marginTop: 24 }}>
        <Col span={24}>
          <Card title="最新告警" style={{ background: '#1e3a5f' }}>
            <RecentAlertsTable />
          </Card>
        </Col>
      </Row>
    </div>
  )
}
```

### 3.2 图表组件

```typescript
// 终端状态饼图
const TerminalStatusPieChart: React.FC = () => {
  const data = [
    { type: '在线', value: 850 },
    { type: '离线', value: 120 },
    { type: '故障', value: 30 },
  ]

  const config = {
    data,
    angleField: 'value',
    colorField: 'type',
    radius: 0.8,
    label: {
      type: 'outer',
      content: '{name} {percentage}',
    },
    interactions: [{ type: 'element-active' }],
    color: ['#52c41a', '#faad14', '#f5222d'],
  }

  return <Pie {...config} />
}

// 设备类型柱状图
const DeviceTypeColumnChart: React.FC = () => {
  const data = [
    { type: 'POS', value: 500 },
    { type: 'KDS', value: 300 },
    { type: '自助点餐机', value: 200 },
  ]

  const config = {
    data,
    xField: 'type',
    yField: 'value',
    color: '#1890ff',
    label: {
      position: 'top',
      style: { fill: '#fff' },
    },
  }

  return <Column {...config} />
}

// 在线趋势折线图
const OnlineTrendLineChart: React.FC = () => {
  const [data, setData] = useState([])

  useEffect(() => {
    // 模拟实时数据更新
    const interval = setInterval(() => {
      setData((prev) => [
        ...prev.slice(-20),
        {
          time: new Date().toLocaleTimeString(),
          value: Math.floor(Math.random() * 100) + 850,
        },
      ])
    }, 3000)

    return () => clearInterval(interval)
  }, [])

  const config = {
    data,
    xField: 'time',
    yField: 'value',
    smooth: true,
    color: '#1890ff',
    areaStyle: { fill: 'l(270) 0:#1890ff 1:#0a1929' },
  }

  return <Line {...config} />
}
```

## 四、告警中心页面

### 4.1 告警列表

```typescript
// src/pages/Monitor/Alerts/index.tsx
import React from 'react'
import { Table, Card, Tag, Button, Space, Form, Select, DatePicker } from 'antd'

const AlertCenter: React.FC = () => {
  const [form] = Form.useForm()

  const columns = [
    {
      title: '告警级别',
      dataIndex: 'level',
      key: 'level',
      render: (level: string) => {
        const levelConfig = {
          CRITICAL: { color: 'red', text: '严重' },
          HIGH: { color: 'orange', text: '高' },
          MEDIUM: { color: 'yellow', text: '中' },
          LOW: { color: 'blue', text: '低' },
        }
        const config = levelConfig[level]
        return <Tag color={config.color}>{config.text}</Tag>
      },
    },
    { title: '告警类型', dataIndex: 'type', key: 'type' },
    { title: '终端ID', dataIndex: 'terminalId', key: 'terminalId' },
    { title: '告警内容', dataIndex: 'message', key: 'message' },
    { title: '发生时间', dataIndex: 'occurredAt', key: 'occurredAt' },
    {
      title: '状态',
      dataIndex: 'status',
      key: 'status',
      render: (status: string) => {
        const statusConfig = {
          OPEN: { color: 'red', text: '未处理' },
          IN_PROGRESS: { color: 'orange', text: '处理中' },
          RESOLVED: { color: 'green', text: '已解决' },
          CLOSED: { color: 'default', text: '已关闭' },
        }
        const config = statusConfig[status]
        return <Tag color={config.color}>{config.text}</Tag>
      },
    },
    {
      title: '操作',
      key: 'action',
      render: (_, record) => (
        <Space>
          {record.status === 'OPEN' && (
            <Button type="link" size="small" onClick={() => handleProcess(record)}>
              处理
            </Button>
          )}
          <Button type="link" size="small" onClick={() => handleViewDetail(record)}>
            详情
          </Button>
          {record.status !== 'CLOSED' && (
            <Button type="link" size="small" onClick={() => handleClose(record)}>
              关闭
            </Button>
          )}
        </Space>
      ),
    },
  ]

  return (
    <Card>
      <Form form={form} layout="inline" style={{ marginBottom: 16 }}>
        <Form.Item name="level" label="告警级别">
          <Select placeholder="请选择" style={{ width: 120 }} allowClear>
            <Select.Option value="CRITICAL">严重</Select.Option>
            <Select.Option value="HIGH">高</Select.Option>
            <Select.Option value="MEDIUM">中</Select.Option>
            <Select.Option value="LOW">低</Select.Option>
          </Select>
        </Form.Item>
        <Form.Item name="status" label="状态">
          <Select placeholder="请选择" style={{ width: 120 }} allowClear>
            <Select.Option value="OPEN">未处理</Select.Option>
            <Select.Option value="IN_PROGRESS">处理中</Select.Option>
            <Select.Option value="RESOLVED">已解决</Select.Option>
          </Select>
        </Form.Item>
        <Form.Item name="dateRange" label="时间范围">
          <DatePicker.RangePicker />
        </Form.Item>
        <Form.Item>
          <Space>
            <Button type="primary">查询</Button>
            <Button>重置</Button>
          </Space>
        </Form.Item>
      </Form>

      <Table columns={columns} rowKey="id" />
    </Card>
  )
}
```

### 4.2 告警处理

```typescript
// src/components/AlertProcessor/index.tsx
import React from 'react'
import { Modal, Form, Input, Select, Button } from 'antd'

interface AlertProcessorProps {
  visible: boolean
  alert: any
  onClose: () => void
  onSubmit: (values: any) => void
}

const AlertProcessor: React.FC<AlertProcessorProps> = ({ visible, alert, onClose, onSubmit }) => {
  const [form] = Form.useForm()

  return (
    <Modal
      title="处理告警"
      open={visible}
      onCancel={onClose}
      onOk={() => form.submit()}
    >
      <Form form={form} layout="vertical" onFinish={onSubmit}>
        <Form.Item label="告警信息">
          <div>{alert?.message}</div>
        </Form.Item>

        <Form.Item name="action" label="处理方式" rules={[{ required: true }]}>
          <Select>
            <Select.Option value="REMOTE_RESTART">远程重启</Select.Option>
            <Select.Option value="MANUAL_FIX">人工修复</Select.Option>
            <Select.Option value="IGNORE">忽略</Select.Option>
          </Select>
        </Form.Item>

        <Form.Item name="comment" label="处理说明" rules={[{ required: true }]}>
          <Input.TextArea rows={4} placeholder="请输入处理说明" />
        </Form.Item>
      </Form>
    </Modal>
  )
}

export default AlertProcessor
```

## 五、统计报表页面

### 5.1 报表展示

```typescript
// src/pages/Monitor/Statistics/index.tsx
import React, { useState } from 'react'
import { Card, Row, Col, DatePicker, Select, Button, Space } from 'antd'
import { Column, Line, Pie } from '@ant-design/charts'

const Statistics: React.FC = () => {
  const [dateRange, setDateRange] = useState<any>([])
  const [dimension, setDimension] = useState('day')

  return (
    <div>
      {/* 筛选条件 */}
      <Card style={{ marginBottom: 16 }}>
        <Space>
          <DatePicker.RangePicker value={dateRange} onChange={setDateRange} />
          <Select value={dimension} onChange={setDimension} style={{ width: 120 }}>
            <Select.Option value="hour">按小时</Select.Option>
            <Select.Option value="day">按天</Select.Option>
            <Select.Option value="week">按周</Select.Option>
            <Select.Option value="month">按月</Select.Option>
          </Select>
          <Button type="primary">查询</Button>
          <Button>导出</Button>
        </Space>
      </Card>

      {/* 统计图表 */}
      <Row gutter={16}>
        <Col span={12}>
          <Card title="终端在线率趋势">
            <OnlineRateTrendChart />
          </Card>
        </Col>
        <Col span={12}>
          <Card title="告警统计">
            <AlertStatisticsChart />
          </Card>
        </Col>
      </Row>

      <Row gutter={16} style={{ marginTop: 16 }}>
        <Col span={12}>
          <Card title="更新任务统计">
            <UpdateTaskStatisticsChart />
          </Card>
        </Col>
        <Col span={12}>
          <Card title="控制任务统计">
            <ControlTaskStatisticsChart />
          </Card>
        </Col>
      </Row>

      <Row gutter={16} style={{ marginTop: 16 }}>
        <Col span={24}>
          <Card title="门店终端分布">
            <StoreTerminalDistributionChart />
          </Card>
        </Col>
      </Row>
    </div>
  )
}
```

## 六、API集成

```typescript
// src/api/monitor.ts
import request from '@/utils/request'

// 查询实时统计
export const getRealtimeStats = () => {
  return request.get('/api/v1/monitor/realtime/stats')
}

// 查询在线趋势
export const getOnlineTrend = (params: { hours: number }) => {
  return request.get('/api/v1/monitor/realtime/trend', { params })
}

// 查询告警列表
export const queryAlerts = (params: any) => {
  return request.get('/api/v1/monitor/alerts', { params })
}

// 处理告警
export const processAlert = (id: string, data: any) => {
  return request.post(`/api/v1/monitor/alerts/${id}/process`, data)
}

// 关闭告警
export const closeAlert = (id: string) => {
  return request.post(`/api/v1/monitor/alerts/${id}/close`)
}

// 查询统计报表
export const getStatistics = (params: any) => {
  return request.get('/api/v1/monitor/statistics', { params })
}

// 导出报表
export const exportStatistics = (params: any) => {
  return request.get('/api/v1/monitor/statistics/export', {
    params,
    responseType: 'blob',
  })
}
```

## 七、WebSocket实时通信

```typescript
// src/utils/websocket.ts
class MonitorWebSocket {
  private ws: WebSocket | null = null
  private reconnectTimer: NodeJS.Timeout | null = null
  private listeners: Map<string, Function[]> = new Map()

  connect(url: string) {
    this.ws = new WebSocket(url)

    this.ws.onopen = () => {
      console.log('WebSocket connected')
      if (this.reconnectTimer) {
        clearTimeout(this.reconnectTimer)
        this.reconnectTimer = null
      }
    }

    this.ws.onmessage = (event) => {
      const data = JSON.parse(event.data)
      const { type, payload } = data

      const listeners = this.listeners.get(type) || []
      listeners.forEach((listener) => listener(payload))
    }

    this.ws.onclose = () => {
      console.log('WebSocket disconnected')
      this.reconnect(url)
    }

    this.ws.onerror = (error) => {
      console.error('WebSocket error:', error)
    }
  }

  reconnect(url: string) {
    this.reconnectTimer = setTimeout(() => {
      console.log('Reconnecting...')
      this.connect(url)
    }, 5000)
  }

  on(type: string, listener: Function) {
    if (!this.listeners.has(type)) {
      this.listeners.set(type, [])
    }
    this.listeners.get(type)!.push(listener)
  }

  off(type: string, listener: Function) {
    const listeners = this.listeners.get(type) || []
    const index = listeners.indexOf(listener)
    if (index > -1) {
      listeners.splice(index, 1)
    }
  }

  disconnect() {
    if (this.ws) {
      this.ws.close()
      this.ws = null
    }
    if (this.reconnectTimer) {
      clearTimeout(this.reconnectTimer)
      this.reconnectTimer = null
    }
  }
}

export default new MonitorWebSocket()
```

---

**版本信息：**
- 文档版本：v1.0
- 创建日期：2026-04-06
