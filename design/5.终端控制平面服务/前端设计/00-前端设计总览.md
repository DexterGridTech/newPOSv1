# TCP管理后台前端设计总览

## 一、系统概述

### 1.1 系统定位

TCP管理后台是面向运营人员和技术支持人员的Web管理平台，用于管理和监控所有终端设备的生命周期、配置、更新和远程控制。

**核心价值：**
- 统一的终端资产管理入口
- 可视化的配置和更新管理
- 实时的设备状态监控
- 便捷的远程运维能力

### 1.2 用户角色

| 角色 | 权限范围 | 主要职责 |
|------|---------|---------|
| 超级管理员 | 全部功能 | 系统配置、用户管理、全局监控 |
| 运营管理员 | 终端管理、配置管理、热更新 | 日常运营、配置下发、版本更新 |
| 技术支持 | 终端查看、远程控制、日志查看 | 故障排查、远程协助 |
| 只读用户 | 查看权限 | 数据查询、报表查看 |

### 1.3 功能模块

```
TCP管理后台
├── 终端管理
│   ├── 终端列表
│   ├── 终端详情
│   ├── 激活管理
│   └── 绑定管理
├── 配置管理
│   ├── 配置模板
│   ├── 配置下发
│   ├── 配置历史
│   └── 配置审计
├── 热更新管理
│   ├── 版本管理
│   ├── 更新任务
│   ├── 灰度发布
│   └── 回滚管理
├── 远程控制
│   ├── 控制任务
│   ├── 任务执行
│   ├── 执行记录
│   └── 审计日志
├── 监控大屏
│   ├── 实时监控
│   ├── 告警中心
│   └── 统计报表
└── 系统管理
    ├── 用户管理
    ├── 角色权限
    └── 操作日志
```

## 二、技术架构

### 2.1 技术栈选型

**前端框架：**
- React 18.x - UI框架
- TypeScript 5.x - 类型系统
- Vite 5.x - 构建工具

**UI组件库：**
- Ant Design 5.x - 企业级UI组件
- Ant Design Pro Components - 高级业务组件
- Ant Design Charts - 数据可视化

**状态管理：**
- Redux Toolkit - 全局状态管理
- RTK Query - 数据获取和缓存

**路由：**
- React Router 6.x - 路由管理

**工具库：**
- Axios - HTTP客户端
- Day.js - 日期处理
- Lodash - 工具函数

### 2.2 项目结构

```
tcp-admin-frontend/
├── src/
│   ├── api/                    # API接口定义
│   │   ├── terminal.ts         # 终端管理API
│   │   ├── config.ts           # 配置管理API
│   │   ├── update.ts           # 热更新API
│   │   └── control.ts          # 远程控制API
│   ├── components/             # 通用组件
│   │   ├── Layout/             # 布局组件
│   │   ├── Charts/             # 图表组件
│   │   └── Common/             # 公共组件
│   ├── pages/                  # 页面组件
│   │   ├── Terminal/           # 终端管理页面
│   │   ├── Config/             # 配置管理页面
│   │   ├── Update/             # 热更新页面
│   │   ├── Control/            # 远程控制页面
│   │   ├── Monitor/            # 监控大屏页面
│   │   └── System/             # 系统管理页面
│   ├── store/                  # Redux状态管理
│   │   ├── slices/             # 状态切片
│   │   └── index.ts            # Store配置
│   ├── hooks/                  # 自定义Hooks
│   ├── utils/                  # 工具函数
│   ├── types/                  # TypeScript类型定义
│   ├── constants/              # 常量定义
│   ├── styles/                 # 全局样式
│   ├── App.tsx                 # 应用入口
│   └── main.tsx                # 主入口
├── public/                     # 静态资源
├── package.json
├── tsconfig.json
└── vite.config.ts
```

### 2.3 架构设计原则

**1. 组件化设计**
- 页面组件：负责页面布局和业务逻辑
- 业务组件：封装特定业务功能
- 通用组件：可复用的UI组件

**2. 状态管理**
- 全局状态：用户信息、权限、主题等
- 页面状态：列表数据、表单数据、UI状态
- 缓存策略：使用RTK Query自动缓存API响应

**3. API设计**
- 统一的请求封装
- 统一的错误处理
- 统一的响应格式
- 自动token刷新

**4. 权限控制**
- 路由级权限：根据角色控制页面访问
- 按钮级权限：根据权限控制操作按钮
- 数据级权限：根据组织范围过滤数据

## 三、设计规范

### 3.1 视觉规范

**色彩系统：**
```typescript
const colors = {
  primary: '#1890ff',      // 主色
  success: '#52c41a',      // 成功
  warning: '#faad14',      // 警告
  error: '#f5222d',        // 错误
  info: '#1890ff',         // 信息
  text: {
    primary: 'rgba(0, 0, 0, 0.85)',
    secondary: 'rgba(0, 0, 0, 0.65)',
    disabled: 'rgba(0, 0, 0, 0.25)',
  },
  background: {
    default: '#f0f2f5',
    paper: '#ffffff',
  },
  border: '#d9d9d9',
}
```

**字体规范：**
```typescript
const typography = {
  h1: { fontSize: 38, fontWeight: 600, lineHeight: 1.23 },
  h2: { fontSize: 30, fontWeight: 600, lineHeight: 1.35 },
  h3: { fontSize: 24, fontWeight: 600, lineHeight: 1.35 },
  h4: { fontSize: 20, fontWeight: 600, lineHeight: 1.4 },
  body1: { fontSize: 14, fontWeight: 400, lineHeight: 1.5 },
  body2: { fontSize: 12, fontWeight: 400, lineHeight: 1.5 },
}
```

**间距规范：**
```typescript
const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
}
```

### 3.2 交互规范

**1. 列表页面**
- 顶部：搜索筛选区域
- 中部：数据表格
- 底部：分页组件
- 右上角：操作按钮（新增、导出等）

**2. 详情页面**
- 顶部：面包屑导航 + 标题
- 左侧：信息卡片
- 右侧：操作区域
- 底部：相关数据标签页

**3. 表单页面**
- 分步表单：复杂流程使用步骤条
- 表单验证：实时验证 + 提交验证
- 错误提示：字段下方显示错误信息
- 操作按钮：底部固定或跟随表单

**4. 反馈机制**
- 成功操作：Toast提示（3秒自动关闭）
- 错误操作：Modal弹窗（需手动关闭）
- 危险操作：二次确认弹窗
- 加载状态：Skeleton占位 + Loading遮罩

### 3.3 响应式设计

**断点定义：**
```typescript
const breakpoints = {
  xs: 0,      // 手机
  sm: 576,    // 平板竖屏
  md: 768,    // 平板横屏
  lg: 992,    // 桌面
  xl: 1200,   // 大桌面
  xxl: 1600,  // 超大桌面
}
```

**适配策略：**
- 桌面端（≥992px）：完整功能，多列布局
- 平板端（768-991px）：简化功能，两列布局
- 移动端（<768px）：核心功能，单列布局

## 四、API集成规范

### 4.1 请求封装

```typescript
// src/utils/request.ts
import axios, { AxiosRequestConfig, AxiosResponse } from 'axios'
import { message } from 'antd'

// 创建axios实例
const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
})

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    // 添加token
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  (response: AxiosResponse) => {
    const { code, data, message: msg } = response.data

    // 成功响应
    if (code === 200) {
      return data
    }

    // 业务错误
    message.error(msg || '请求失败')
    return Promise.reject(new Error(msg))
  },
  (error) => {
    // 网络错误
    if (error.response) {
      const { status, data } = error.response

      switch (status) {
        case 401:
          message.error('未授权，请重新登录')
          // 跳转到登录页
          window.location.href = '/login'
          break
        case 403:
          message.error('没有权限访问')
          break
        case 404:
          message.error('请求的资源不存在')
          break
        case 500:
          message.error('服务器错误')
          break
        default:
          message.error(data?.message || '请求失败')
      }
    } else {
      message.error('网络连接失败')
    }

    return Promise.reject(error)
  }
)

export default request
```

### 4.2 API响应格式

**统一响应格式：**
```typescript
interface ApiResponse<T = any> {
  code: number          // 状态码：200成功，其他失败
  message: string       // 响应消息
  data: T              // 响应数据
  timestamp: string    // 响应时间
}

// 分页响应格式
interface PageResponse<T> {
  items: T[]           // 数据列表
  total: number        // 总数
  page: number         // 当前页
  size: number         // 每页大小
  totalPages: number   // 总页数
}
```

### 4.3 错误处理

**错误码定义：**
```typescript
enum ErrorCode {
  SUCCESS = 200,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  INTERNAL_ERROR = 500,

  // 业务错误码
  TERMINAL_NOT_FOUND = 1001,
  TERMINAL_ALREADY_ACTIVATED = 1002,
  CONFIG_NOT_FOUND = 2001,
  CONFIG_VERSION_CONFLICT = 2002,
  UPDATE_TASK_NOT_FOUND = 3001,
  UPDATE_TASK_ALREADY_STARTED = 3002,
  CONTROL_TASK_NOT_FOUND = 4001,
  CONTROL_TASK_TIMEOUT = 4002,
}
```

## 五、性能优化

### 5.1 代码分割

```typescript
// 路由懒加载
import { lazy } from 'react'

const TerminalList = lazy(() => import('@/pages/Terminal/List'))
const ConfigManagement = lazy(() => import('@/pages/Config/Management'))
const UpdateManagement = lazy(() => import('@/pages/Update/Management'))
const ControlManagement = lazy(() => import('@/pages/Control/Management'))
```

### 5.2 数据缓存

```typescript
// 使用RTK Query自动缓存
import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react'

export const terminalApi = createApi({
  reducerPath: 'terminalApi',
  baseQuery: fetchBaseQuery({ baseUrl: '/api/v1' }),
  tagTypes: ['Terminal'],
  endpoints: (builder) => ({
    getTerminals: builder.query({
      query: (params) => ({
        url: '/terminals',
        params,
      }),
      providesTags: ['Terminal'],
      // 缓存5分钟
      keepUnusedDataFor: 300,
    }),
  }),
})
```

### 5.3 虚拟滚动

```typescript
// 大数据量列表使用虚拟滚动
import { List } from 'react-virtualized'

<List
  width={800}
  height={600}
  rowCount={data.length}
  rowHeight={50}
  rowRenderer={({ index, key, style }) => (
    <div key={key} style={style}>
      {data[index]}
    </div>
  )}
/>
```

## 六、安全规范

### 6.1 XSS防护

```typescript
// 使用DOMPurify清理HTML
import DOMPurify from 'dompurify'

const sanitizedHTML = DOMPurify.sanitize(userInput)
```

### 6.2 CSRF防护

```typescript
// 请求头添加CSRF Token
request.interceptors.request.use((config) => {
  const csrfToken = document.querySelector('meta[name="csrf-token"]')?.getAttribute('content')
  if (csrfToken) {
    config.headers['X-CSRF-Token'] = csrfToken
  }
  return config
})
```

### 6.3 敏感信息保护

```typescript
// 不在localStorage存储敏感信息
// 使用httpOnly cookie存储token
// 敏感操作需要二次验证
```

## 七、测试规范

### 7.1 单元测试

```typescript
// 使用Vitest + React Testing Library
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import TerminalList from './TerminalList'

describe('TerminalList', () => {
  it('should render terminal list', () => {
    render(<TerminalList />)
    expect(screen.getByText('终端列表')).toBeInTheDocument()
  })
})
```

### 7.2 E2E测试

```typescript
// 使用Playwright
import { test, expect } from '@playwright/test'

test('terminal management flow', async ({ page }) => {
  await page.goto('/terminals')
  await page.click('text=新增终端')
  await page.fill('input[name="terminalId"]', 'T001')
  await page.click('button[type="submit"]')
  await expect(page.locator('text=创建成功')).toBeVisible()
})
```

## 八、部署方案

### 8.1 构建配置

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: 'dist',
    sourcemap: false,
    minify: 'terser',
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom', 'react-router-dom'],
          'antd-vendor': ['antd', '@ant-design/icons'],
          'redux-vendor': ['@reduxjs/toolkit', 'react-redux'],
        },
      },
    },
  },
})
```

### 8.2 环境配置

```bash
# .env.development
VITE_API_BASE_URL=http://localhost:8080/api
VITE_WS_URL=ws://localhost:8080/ws

# .env.production
VITE_API_BASE_URL=https://api.example.com/api
VITE_WS_URL=wss://api.example.com/ws
```

### 8.3 Nginx配置

```nginx
server {
    listen 80;
    server_name tcp-admin.example.com;

    root /usr/share/nginx/html;
    index index.html;

    # SPA路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API代理
    location /api {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # WebSocket代理
    location /ws {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## 九、文档导航

本前端设计文档分为以下模块，每个模块单独成文：

1. **00-前端设计总览.md**（本文档）- 技术架构、设计规范、API集成
2. **01-终端管理模块.md** - 终端列表、详情、激活、绑定功能设计
3. **02-配置管理模块.md** - 配置模板、下发、历史、审计功能设计
4. **03-热更新管理模块.md** - 版本管理、更新任务、灰度发布、回滚功能设计
5. **04-远程控制模块.md** - 控制任务、执行记录、审计日志功能设计
6. **05-监控大屏模块.md** - 实时监控、告警中心、统计报表功能设计
7. **06-系统管理模块.md** - 用户管理、角色权限、操作日志功能设计
8. **07-通用组件库.md** - 可复用组件设计和使用说明

---

**版本信息：**
- 文档版本：v1.0
- 创建日期：2026-04-06
- 最后更新：2026-04-06
