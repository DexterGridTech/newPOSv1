import {createModuleCommands, defineCommand} from "@impos2/kernel-core-base";
import {moduleName} from "../../moduleName";

export const kernel{{PACKAGE_NAME_PASCAL}}Commands = createModuleCommands(moduleName,{
})

