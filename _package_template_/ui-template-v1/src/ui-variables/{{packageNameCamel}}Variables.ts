import { registerUIVariable } from "@impos2/kernel-base";

/**
 * {{displayName}} 模块的 UI 变量
 *
 * 用于在不同组件之间共享状态
 */
export const {{packageNameCamel}}Variable = {
    // 在这里添加你的 UI 变量
    // 例如: myVariable: registerUIVariable({key: '{{moduleName}}.myVariable', defaultValue: ''}),
};
