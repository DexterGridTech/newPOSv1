{
  "name": "@impos2/kernel-base-transport-runtime",
  "version": "1.0.0",
  "description": "Kernel base transport runtime package",
  "author": "DexterYang",
  "license": "ISC",
  "exports": {
    ".": "./src/index.ts"
  },
  "react-native": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "build-for-product": "rm -rf dist && tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:scenario": "vitest run",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "check:circular": "madge --circular --extensions ts --warning src/"
  },
  "dependencies": {
    "@impos2/kernel-base-contracts": "workspace:*",
    "@impos2/kernel-base-platform-ports": "workspace:*"
  },
  "devDependencies": {
    "@impos2/kernel-server-config-v2": "workspace:*",
    "@types/node": "^25.0.9",
    "@types/ws": "^8.5.13",
    "madge": "^8.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "ws": "^8.18.0"
  }
}
