{
  "name": "@next/kernel-base-tdp-sync-runtime-v2",
  "version": "1.0.0",
  "description": "Kernel base tdp sync runtime v2 package",
  "author": "DexterYang",
  "license": "ISC",
  "exports": {
    ".": "./src/index.ts"
  },
  "react-native": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "build-for-product": "rm -rf dist && tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:scenario": "vitest run",
    "clean": "rm -rf dist",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "check:circular": "madge --circular --extensions ts --warning src/"
  },
  "dependencies": {
    "@next/kernel-base-contracts": "workspace:*",
    "@next/kernel-base-runtime-shell-v2": "workspace:*",
    "@next/kernel-base-state-runtime": "workspace:*",
    "@next/kernel-base-tcp-control-runtime-v2": "workspace:*",
    "@next/kernel-base-topology-runtime-v3": "workspace:*",
    "@next/kernel-base-transport-runtime": "workspace:*",
    "@next/kernel-server-config-v2": "workspace:*",
    "@reduxjs/toolkit": "^2.10.0"
  },
  "devDependencies": {
    "@next/kernel-base-platform-ports": "workspace:*",
    "@types/node": "^25.0.9",
    "madge": "^8.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
