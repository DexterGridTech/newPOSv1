/**
 * Kernel layer rule:
 * - do not depend on React here
 * - do not implement React hooks in 1-kernel
 * - keep this file only as a rule marker and export placeholder
 * - selectors are the public read path for UI/runtime consumers
 */
export {}
