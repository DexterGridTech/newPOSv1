import {packageVersion} from '../src/index'

console.log('[workspace-package-scaffolder]', {
  packageName: '@impos2/kernel-base-contracts',
  packageVersion,
  message: 'Replace this dev entry with package-specific verification when implementation starts.',
})
