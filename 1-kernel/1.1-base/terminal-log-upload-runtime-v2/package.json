{
  "name": "@impos2/kernel-base-terminal-log-upload-runtime-v2",
  "version": "1.0.0",
  "description": "Kernel base terminal log upload runtime v2 package",
  "author": "DexterYang",
  "license": "ISC",
  "exports": {
    ".": "./src/index.ts"
  },
  "react-native": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "build-for-product": "rm -rf dist && tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:scenario": "vitest run",
    "clean": "rm -rf dist",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@impos2/kernel-base-contracts": "workspace:*",
    "@impos2/kernel-base-platform-ports": "workspace:*",
    "@impos2/kernel-base-runtime-shell-v2": "workspace:*",
    "@impos2/kernel-base-tcp-control-runtime-v2": "workspace:*",
    "@impos2/kernel-base-topology-runtime-v3": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
