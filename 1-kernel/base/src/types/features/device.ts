export interface DeviceInfo {
    id: string;
    manufacturer: string;
    os: string;
    osVersion: string;
    cpu: string;
    memory: string;
    disk: string;
    network: string;
    displays: string[];
}