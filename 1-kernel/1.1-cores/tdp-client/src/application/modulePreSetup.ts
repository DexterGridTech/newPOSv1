import type {ApplicationConfig, AppModule} from '@impos2/kernel-core-base'

export const kernelCoreTdpClientModulePreSetup = async (
  _config: ApplicationConfig,
  _allModules: AppModule[],
) => {
  return
}
