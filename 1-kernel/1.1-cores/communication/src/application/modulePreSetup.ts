import {ApplicationConfig, AppModule} from '@impos2/kernel-core-base'

export const kernelCoreCommunicationModulePreSetup = async (
  _config: ApplicationConfig,
  _allModules: AppModule[],
) => {
  return
}
