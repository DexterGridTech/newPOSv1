export enum InstanceMode {
    MASTER = 'master',
    SLAVE = 'slave'
}

export enum DisplayMode {
    PRIMARY = 'primary',
    SECONDARY = 'secondary'
}

export enum WorkSpace {
    MAIN = 'main',
    BRANCH = 'branch'
}