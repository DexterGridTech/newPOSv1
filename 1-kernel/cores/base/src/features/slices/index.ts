import {requestStatusConfig} from "./requestStatus";
import {systemParametersConfig} from "./systemParameters";
import {errorMessagesConfig} from "./errorMessages";


export const kernelCoreBaseSlice = {
    requestStatus:requestStatusConfig,
    systemParameters:systemParametersConfig,
    errorMessages:errorMessagesConfig
}