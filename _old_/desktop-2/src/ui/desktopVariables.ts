import {registerUIVariable} from "_old_/base";
import {EmptyScreen} from "_old_/base-2";

export const desktopVariables = {
    rootScreenContainer: registerUIVariable({key: 'screen.container.root', defaultValue: EmptyScreen})
}